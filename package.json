{
  "name": "myapp1",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "npx tsx server/index.ts",
    "build": "cd client && npm install && npm run build",
    "start": "npx tsx server/index.ts"
  },
  "dependencies": {
    "express": "^4.21.2",
    "drizzle-orm": "^0.36.0",
    "zod": "^3.23.2",
    "drizzle-zod": "^0.8.3"
  },
  "devDependencies": {
    "tsx": "^3.12.7"
  }
}
